import { 
    Router,
    Request,
    Response
} from "express";
import * as scheduleController from "../controllers/schedule-controller.js";

const router = Router();

// router.get('/', scheduleController.);