import { 
    Router,
    Request,
    Response
} from "express";
import * as scheduleController from "../controllers/schedule-controller";

const router = Router();

// router.get('/', scheduleController.);